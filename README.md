# Monotonic Stack Allocator
Implementation of a simple monotonic (linear) allocator on the stack.

The implementation can be found in `stackallocator.h`. See `example.cpp` for some minimal usage examples.
